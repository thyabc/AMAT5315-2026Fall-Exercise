# Part 3: approved test-first sequence

This records the implementation sequence agreed in the conversation. The RED
commit must precede any simulator implementation.

1. Add `week2/md/tests/dimer.rs` using the intended public interface. Run it and
   observe failure because the simulator API does not yet exist. Commit the test.
2. Implement `System`, analytical vector forces, cached accelerations, the shared
   trait, forward Euler, velocity-Verlet, and the common dimer experiment driver.
3. Preserve all existing tests. Check vector-force direction and Newton's third
   law, kinetic energy, free flight, Euler's old-velocity update, and Verlet time
   reversal. Run the dimer acceptance tests, including the 5000-step bound.
4. Add CSV output and a pytest check of the executable's output contract. Format
   Rust code, run tests, and commit GREEN.
5. Add the plotting example, generate and inspect the two-panel PNG, and document
   exact reproduction commands and measured errors. Commit plot and documentation
   separately. Run final formatting and tests.

Execution evidence: RED commit `1156102` fails with E0432 for missing `Euler`,
`VelocityVerlet`, `EnergySample`, and `run_dimer`. This is the expected compile-time
failure, before any simulator code was added. The acceptance assertions are retained
through GREEN (only rustfmt layout changes).
